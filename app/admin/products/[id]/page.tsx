import { notFound } from "next/navigation";
import Link from "next/link";
import { getAdminCategories, getAdminProduct } from "@/lib/admin/service";
import { AdminProductForm } from "@/components/admin-product-form";
export default async function EditProductPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [product, categories] = await Promise.all([getAdminProduct(id), getAdminCategories()]); if (!product) notFound(); return <><div className="admin-page-heading"><div><p className="admin-kicker">Catalogue</p><h1>Edit product</h1><p>Updated {product.updatedAt.toLocaleDateString("en-IN")}.</p></div><Link className="admin-button admin-button-quiet" href="/admin/products">Back</Link></div><AdminProductForm product={product} categories={categories} /></>; }