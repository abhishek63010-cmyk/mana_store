import Link from "next/link";
import { getAdminCategories } from "@/lib/admin/service";
export default async function AdminCategoriesPage() { const categories = await getAdminCategories(); return <><div className="admin-page-heading"><div><p className="admin-kicker">Catalogue structure</p><h1>Categories</h1><p>Control the active categories available to customer browsing.</p></div><Link className="admin-button" href="/admin/categories/new">New category</Link></div><section className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Name</th><th>Slug</th><th>Status</th><th>Products</th><th>Updated</th></tr></thead><tbody>{categories.map((category) => <tr key={category.id}><td><Link href={`/admin/categories/${category.id}`}><strong>{category.name}</strong></Link></td><td>{category.slug}</td><td><span className={`admin-status ${category.active ? "status-published" : "status-hidden"}`}>{category.active ? "Active" : "Inactive"}</span></td><td>{category._count.products}</td><td>{category.updatedAt.toLocaleDateString("en-IN")}</td></tr>)}{categories.length === 0 && <tr><td colSpan={5}><div className="admin-empty">No categories yet.</div></td></tr>}</tbody></table></section></>; }