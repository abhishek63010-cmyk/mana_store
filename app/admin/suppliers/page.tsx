import { getAdminSuppliers } from "@/lib/admin/service";
export default async function AdminSuppliersPage() { const suppliers = await getAdminSuppliers(); return <><div className="admin-page-heading"><div><p className="admin-kicker">Future integrations</p><h1>Suppliers</h1><p>Read-only supplier registry. API credentials and synchronization remain server-side and are not displayed here.</p></div></div><section className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Supplier</th><th>Key</th><th>Status</th><th>Products</th><th>Supplier orders</th><th>Sync logs</th></tr></thead><tbody>{suppliers.map((supplier) => <tr key={supplier.id}><td><strong>{supplier.name}</strong><small>Added {supplier.createdAt.toLocaleDateString("en-IN")}</small></td><td>{supplier.code}</td><td><span className={`admin-status ${supplier.active ? "status-published" : "status-hidden"}`}>{supplier.active ? "Active" : "Inactive"}</span></td><td>{supplier._count.supplierProducts}</td><td>{supplier._count.supplierOrders}</td><td>{supplier._count.syncLogs}</td></tr>)}{suppliers.length === 0 && <tr><td colSpan={6}><div className="admin-empty">No suppliers configured.</div></td></tr>}</tbody></table></section></>; }